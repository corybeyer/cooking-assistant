# Database Configuration
DB_SERVER=sql-cooking-assistant.database.windows.net
DB_NAME=db-cooking-assistant
DB_USER=sqldb-cookingassistant
DB_PASSWORD=<your-password-here>

# Claude API
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Note: Authentication handled by Azure Container Apps Easy Auth (Entra ID)
# See: https://learn.microsoft.com/en-us/azure/container-apps/authentication

# Azure Speech (Phase 4)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=centralus

# Azure Communication Services (SMS)
# Option 1: Managed Identity (preferred in Azure)
AZURE_COMM_ENDPOINT=https://<resource-name>.communication.azure.com
# Option 2: Connection String (for local development)
# AZURE_COMM_CONNECTION_STRING=endpoint=https://...;accesskey=...
# Sender phone number (required - must be purchased in Azure portal)
AZURE_COMM_SENDER_NUMBER=+1XXXXXXXXXX

# App URL (for shareable links in SMS)
APP_BASE_URL=https://your-app.azurecontainerapps.io
